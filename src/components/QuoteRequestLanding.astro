---
import type { LocaleKey } from '../types/site';
import QuotePage from '../features/quote/components/QuotePage.astro';

interface Props {
	locale: LocaleKey;
}

const { locale } = Astro.props as Props;
---

<QuotePage locale={locale} />
