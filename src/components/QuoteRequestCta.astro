---
import type { LocaleKey } from '../types/site';
import QuotePageCta from '../features/quote/components/QuotePageCta.astro';

interface Props {
	locale: LocaleKey;
}

const { locale } = Astro.props as Props;
---

<QuotePageCta locale={locale} />
